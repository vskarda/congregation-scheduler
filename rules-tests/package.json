{
  "name": "rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security rules tests (run against the emulator)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-rules-test \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.6.0",
    "firebase-tools": "^14.19.1"
  }
}
