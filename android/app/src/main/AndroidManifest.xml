<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Firebase (Auth + Firestore) needs network access in release builds;
         only the debug/profile manifests add it automatically. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Local assignment reminders (flutter_local_notifications): show
         notifications, and re-schedule pending ones after a device reboot. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- open_filex declares READ_MEDIA_IMAGES/VIDEO/AUDIO in its library
         manifest so it can open files from shared storage. This app only ever
         hands it a file it just wrote into its own cache directory
         (getTemporaryDirectory()) — see file_opener_io.dart and
         backup_download_io.dart — where the plugin's pathRequiresPermission()
         returns false and no runtime permission is ever requested. Strip the
         declarations so the merged manifest satisfies Google Play's photo and
         video permissions policy; system pickers are sufficient here, since
         file_picker uses ACTION_OPEN_DOCUMENT / ACTION_GET_CONTENT.
         Enforced by a gate in .github/workflows/release.yml — if you ever need
         to open a file from shared storage, that gate must change too. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"
        tools:node="remove"/>
    <!-- usesCleartextTraffic: already the platform default at this targetSdk;
         declared so a merged library manifest cannot opt the app back in.
         The backup rules keep the Firestore offline cache (which holds the
         congregation's private profiles) out of cloud backup and device
         transfer — see android/app/src/main/res/xml/backup_rules.xml. -->
    <application
        android:label="Congregation Scheduler"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications: delivers scheduled reminders and
             re-schedules them after a reboot / app update. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets add_2_calendar find the calendar app to hand new events off to. -->
        <intent>
            <action android:name="android.intent.action.INSERT"/>
            <data android:mimeType="vnd.android.cursor.item/event"/>
        </intent>
    </queries>
</manifest>
