<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (android:dataExtractionRules). Keep in sync with
  backup_rules.xml, which covers Android 11 and older.

  See backup_rules.xml for why the "database" domain (the Firestore offline
  cache) is excluded. Device-to-device transfer is covered as well as cloud
  backup: handing a phone on is exactly the case where the previous user's
  cached congregation data should not travel with it.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
    </device-transfer>
</data-extraction-rules>
